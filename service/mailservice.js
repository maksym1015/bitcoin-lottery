import { API_BASE_URL, SEC_TOKEN } from './config';

const BASE_URL = `${API_BASE_URL}/mailservice/`;

export const requestResetPwd = (memberID, brandID, email, actionLink, lang) => {

}

export const sendWelcome = (memberID, brandID, email, lang) => {

}